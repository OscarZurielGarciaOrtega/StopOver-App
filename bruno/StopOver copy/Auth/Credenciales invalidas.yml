info:
  name: Credenciales invalidas
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/auth/login"
  body:
    type: json
    data: '{ "email": "noesunemail", "password": "" }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
