info:
  name: Login Propietario
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "paola.sanchez@stopover.com",
        "password": "Operador123!"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("tokenPropietario", res.body.token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
