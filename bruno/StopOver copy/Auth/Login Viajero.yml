info:
  name: Login Viajero
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "ana.garcia@stopover.com",
        "password": "Usuario123!"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("tokenViajero", res.body.token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
