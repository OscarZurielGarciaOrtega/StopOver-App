info:
  name: Aprobados
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/negocios/aprobados"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
