info:
  name: Aprobar negocio
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseUrl}}/api/admin/negocios/1/aprobar"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
