info:
  name: Pendientes (admin)
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/admin/negocios/pendientes"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
