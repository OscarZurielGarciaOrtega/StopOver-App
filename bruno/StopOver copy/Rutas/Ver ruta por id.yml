info:
  name: Ver ruta por id
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/rutas/2"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
