info:
  name: Listar usuarios (ADMIN)
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/usuarios"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
